# Lawyer-App

Backend API for the Lawyer consultation app (Node.js, Express, MongoDB, Socket.IO).

## Repository layout

- `backend/`: the API server
- `.github/workflows/deploy-backend.yml`: deploys `backend/` to AWS EC2 when a push to `main` changes `backend/` or this workflow
